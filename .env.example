# Live desk (published digest) - see src/PortfolioDigest.tsx
# URL + anon key of the Supabase project that holds pc_digest_public.
# The anon key is public by design; access is enforced by Postgres grants.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
